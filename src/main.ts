import { createApp } from 'vue'
import App from './App.vue'
import './style.scss'

const app = createApp(App)
app.mount('#app')
console.log(import.meta.env)

// css,js,pic,html
// sass
// 插入base.scss
// axios
// 插入vconsole
// vue-tsc,ts
// eslint，prettier

// commit，git commit

// router
// 按需引入
// pinia，通信
// 复合函数
